using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Baruch
{
    public class LevelTimePerceptron : MonoBehaviour
    {
        private void Reset()
        {
            name = nameof(LevelTimePerceptron);
        }
    }
}
